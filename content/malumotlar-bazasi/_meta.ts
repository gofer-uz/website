import type { MetaRecord } from 'nextra'

const meta: MetaRecord = {
  index: "Kirish",
  postgresql: "PostgreSQL",
  // Kelajakda boshqa ma'lumotlar bazalari shu yerga sibling papka bo'lib qo'shiladi, masalan:
  // mongodb: "MongoDB",
  // sqlite: "SQLite",
}

export default meta
